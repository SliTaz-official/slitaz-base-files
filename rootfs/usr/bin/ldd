#!/bin/sh
#
# Tiny ldd fake.
# Copyright (C) 2010-2015 SliTaz GNU/Linux.
#

echo
file="$@"
for file do
	case "$file" in
		*/*) ;;
		*) [ -f "./$file" ] && file="./$file"
	esac
	which "$file" | sed 's%^./%%;s%$%:%'
	LD_TRACE_LOADED_OBJECTS=1 "$file"
	echo
done
